h1. ToDo

* Evaluate using FileMonitor over sketches.
* Add server method to Object... so that from within an irb session one could type 'server' which would launch a background process (reporting to the console).  This method would would be dependent upon the flavor of the framework...
* Ensure that sever launched from within irb is restarted when file changes are detected.
* irbrc Update procedure from within irb